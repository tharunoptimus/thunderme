extends layouts/main-layout.pug

block content
    script.
        var postId = '!{postId}'
    .postsContainerContainer.viewPage
        .postsContainer

    createPostModals(userLoggedIn)

block scripts 
    script(src="/js/postPage.js")